import { createTheme } from "@mui/material";
const theme = createTheme({
  palette: {
    primary: {
      main: "#1e40af",
    },
    secondary: {
      main: "#ff4081",
    },
  },
});
export default theme;
